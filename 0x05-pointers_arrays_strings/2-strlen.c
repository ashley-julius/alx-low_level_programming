/* This function returns the length of a string */
#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: pointer to a char
 *Return: the lenght
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
