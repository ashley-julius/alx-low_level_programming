/* This program checks for letter character */

#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * * _isalpha- checks for letter
 * * characters and return 1 or 0
 * * @c: function takes one argument
 * * Return: zero
 */
int _isalpha(int c)
{
	return (0);
	_putchar('\n');
}

