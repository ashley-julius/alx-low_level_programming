/* This program prints exactly a line of text */
#include <stdio.h>
/**
 * main- without the main function, our C program is not functionable
 * @void:represents that the function takes no argument
 *Return:returns zero, which is used passed to the exit status value as zero
 */
int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
